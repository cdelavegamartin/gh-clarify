<!--
Template for a "code clarification" GitHub Discussion.
Category: Q&A
Labels: code-clarification

Fill in every section. The permalink + snippet make the question resilient to
commits moving the code around: if the file/line drift, the snippet is still
searchable.
-->

## Question

<!-- Terse, clear, single-sentence question. This becomes the discussion title too. -->

## Location

- **File:** `path/to/file.ext`
- **Line(s):** `<start>`-`<end>` (as of commit `<short-sha>`)
- **Permalink:** `https://github.com/<owner>/<repo>/blob/<full-sha>/path/to/file.ext#L<start>-L<end>`

## Code snippet

```<language>
<exact snippet, copied verbatim, so it can be located via search if the
permalink's line numbers no longer match>
```

## Context

<!--
Why you were looking at this, what you were trying to do, and what
specifically is unclear (e.g. "why this call and not X", "why is this check
necessary", "is this defensive or load-bearing").
-->
